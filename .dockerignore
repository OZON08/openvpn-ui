# Exclude vendored dependencies — the binary is built via go mod download in CI
vendor/

# Build tooling and docs (kept: build/assets needed by Dockerfile.ci)
build/*.sh
build/Dockerfile*
docs/
.github/
*.md
swagger/

# Development artefacts
*.db
*.log
.git/
